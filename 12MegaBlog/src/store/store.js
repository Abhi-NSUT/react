import{configureStore} from '@reduxjs/toolkit';
const store=configureStore({
    reducer:{}
        
})
export default store;
// This is a Redux store configuration file. It sets up the store using Redux Toolkit's configureStore function. 
// The store is exported for use in the application, allowing components to access and manage application state.
// The store can be extended with reducers and middleware as needed for the application.